[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "isaaclab_ovsensors"
version = "0.1.0"
description = "ovsensors renderer backend for Isaac Lab"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
